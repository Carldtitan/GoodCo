"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function CancelRequestButton({ requestId }: { requestId: string }) { const router = useRouter(); const [pending, setPending] = useState(false); const [error, setError] = useState<string | null>(null); async function cancel() { setPending(true); setError(null); const response = await fetch(`/api/marketplace/requests/${requestId}/cancellation`, { method: "POST", headers: { "Content-Type": "application/json" }, body: "{}" }); const data = await response.json() as { error?: string }; if (!response.ok) setError(data.error ?? "Could not cancel."); else router.refresh(); setPending(false); } return <div className="flex items-center gap-2"><button className="min-h-11 rounded-panel border border-border px-3 text-sm font-semibold" disabled={pending} onClick={cancel} type="button">{pending ? "Cancelling…" : "Cancel"}</button>{error ? <span className="text-sm text-danger">{error}</span> : null}</div>; }
